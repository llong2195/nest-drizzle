export enum EnvEnum {
  Dev = 'development',
  Staging = 'staging',
  Production = 'production',
}

export enum LOCALES {
  EN = 'en-US',
  VI = 'vi',
}

export enum VerifyKycEnum {
  VERIFY = 1,
  PEDDING = 2,
  REJECT = 4,
}
